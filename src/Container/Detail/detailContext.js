import React from 'react';
import { EMPTY_FUNC, EMPTY_MAP } from '../../constants';

// ================================================

export const DetailContext = React.createContext({
  equipment: EMPTY_MAP,
});
